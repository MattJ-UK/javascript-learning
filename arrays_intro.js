// Arrays are flexible, can contain multiple types
// Best to stick to single type though


var shoppinglist =['bread', 'eggs', 'milk', 'butter'];
console.log(shoppinglist);

// Zero indexed

listOne = shoppinglist[0];
console.log(listOne);

shoppinglist[1] = 'cheese';

console.log(shoppinglist);

console.log(shoppinglist.length);
//add item
shoppinglist.push('apples')

console.log(shoppinglist);

// remove it
console.log(shoppinglist.pop());

